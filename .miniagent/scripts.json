{
  "scripts": [
    {
      "name": "build",
      "command": "go build ./...",
      "description": "编译全部包（go build ./...）"
    },
    {
      "name": "vet",
      "command": "go vet ./...",
      "description": "静态检查全部包（go vet ./...）"
    },
    {
      "name": "test",
      "command": "go test ./...",
      "description": "运行全部单元测试（go test ./...）"
    },
    {
      "name": "race",
      "command": "go test -race ./...",
      "description": "带竞态检测运行全部测试（go test -race ./...）"
    },
    {
      "name": "fmt",
      "command": "gofmt -l .",
      "description": "列出未按 gofmt 格式化的文件（空输出表示已格式化）"
    },
    {
      "name": "bridge_test",
      "command": "go test ./internal/miniagent/... ./internal/miniclient/... ./cmd/miniagent-back/...",
      "description": "测试 miniagent 后端 ↔ miniclient 子进程耦合（改这两处后优先跑）"
    }
  ]
}
