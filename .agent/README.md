# .agent — 项目级 Agent 记忆

本目录存放 `lark-bridge` 项目级 Agent 记忆，分三层：

- `L0/`：永久约束，每次会话加载。
- `L1/`：过程上下文，任务结束归档。
- `L2/`：经验教训与可复用知识。

## 维护约定

1. AGENT 与用户均可读写。
2. L0 更新需用户明确授权或手动编辑。
3. 文件使用 Markdown + YAML frontmatter。
4. 任务完成后归档 L1，必要时沉淀 L2。
5. 检索优先用精确关键词与标签，必要时辅以语义搜索。
